rootProject.name = 'solr-actuators'

include ':solr-actuators'

include ':integration-tests'
["solr6"].each { version ->
    include ":integration-tests:${version}"
    include ":integration-tests:${version}"
}